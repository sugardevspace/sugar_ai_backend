version: "3.9"

services:
  fastapi:
    build: .
    container_name: sugarai-fastapi
    ports:
      - "8080:8080"
    env_file:
      - .env.local            # ← loads every key in .env
    restart: unless-stopped
